name = "morgus-mcp-code-executor"
main = "src/index.ts"
compatibility_date = "2024-11-01"

[vars]
ENVIRONMENT = "production"

# Secrets (set via wrangler secret put):
# - E2B_API_KEY (for sandboxed code execution)
# - SUPABASE_URL
# - SUPABASE_SERVICE_KEY
